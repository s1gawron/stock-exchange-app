export interface FavouriteStockRowDTO {
    ticker: string;
    companyFullName: string;
    currentPrice: number;
    priceChange: number;
    percentagePriceChange: number;
    stockCurrency: string;
}
